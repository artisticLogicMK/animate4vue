import { Options, AttentionOptions } from './types';
export declare const isValidOptions: (options: Options) => boolean;
export declare const isValidAttOptions: (options: AttentionOptions) => boolean;
